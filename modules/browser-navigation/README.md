# browser.navigation

Строит проверяемый план открытия обычного HTTP(S) URL или поисковой страницы.
Запрещены `file:`, `javascript:`, URL с логином/паролем и неизвестные поисковые
движки. Фактическое открытие передаётся системному браузеру.
