[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-native-linux-provider-ollama"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["zstandard>=0.23,<1"]

[tool.setuptools.packages.find]
where = ["src"]
