[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-native-linux-documents-pdf"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pypdf>=5,<7"]

[tool.setuptools.packages.find]
where = ["src"]
