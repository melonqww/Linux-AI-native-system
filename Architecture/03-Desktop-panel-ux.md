# UX правой AI-панели — v0.1

## Назначение

Панель — основной интерфейс системного AI-помощника. Это компактное окно в правом нижнем углу: примерно половина высоты экрана, но не его полный край. Оно не заменяет рабочий стол, верхнюю панель GNOME или стандартные уведомления.

При первом входе в Ubuntu панель открывается автоматически. Позже она вызывается горячей клавишей, значком в верхней панели и кликом по уведомлению AI.

## Компоновка

```text
┌────────────────────────────────────────────┐
│ [☰ Боковая панель][Новый чат][⚙ Настройки] │
│ Новый чат                                  │
│ ▰ ~/Projects/Linux AI System                │
├────────────────────────────────────────────┤
│                                            │
│  AI отвечает обычным белым текстом.         │
│                                            │
│                  [Сообщение пользователя]  │
│                                            │
│  Карточка плана или результата действия.    │
│                                            │
├────────────────────────────────────────────┤
│ Сообщение для вашего ИИ                     │
│ [Подтверждать за меня] [Модель⌄]      (↑)  │
└────────────────────────────────────────────┘
```

- Панель имеет тёмно-серый фон, светлый текст и минимум декоративных элементов. В верхней части находятся три равные по ширине вкладки. Только `☰ Боковая панель` визуально прилегает к левому боку; `Новый чат` и `⚙ Настройки` прилегают к верхней границе и имеют лёгкое скругление правого верхнего угла. Кнопка `＋` находится отдельно от вкладок в увеличенном круглом фоне.
- При наведении или фокусе на `＋` появляется подсказка «Создать новый чат» под кнопкой.
- Пока открыт один чат, `＋` не создаёт второй, а показывает краткое уведомление внизу окна. После закрытия текущего чата можно создать новый.
- Сообщения пользователя — белые овальные карточки с тёмным текстом, выровненные вправо.
- Ответы AI — белый текст без отдельного пузыря, выровненный влево.
- Поле ввода всегда закреплено снизу. Оно растёт по высоте до шести строк; в нём есть иконка щита для режима «Подтверждать за меня», правый селектор модели с выпадающим списком и круглая кнопка отправки.
- Путь под названием чата отражает текущую рабочую директорию задачи. Это контекст, а не неограниченное разрешение читать её содержимое.

## Верхняя панель

| Элемент | Поведение v0.1 |
|---|---|
| `☰` | Открывает список чатов; до появления истории показывает краткий список-заглушку. |
| Название | По умолчанию «Новый чат», позднее становится кратким названием задачи. |
| Директория | Динамически показывает текущую директорию workspace. |
| `⚙` | Полностью заменяет область чата экраном настроек. |

## Настройки и фоновая генерация

Экран настроек и боковая панель не открываются поверх чата: они заменяют содержимое окна. При этом запрос к модели не отменяется; когда пользователь возвращается в чат, готовый ответ появляется в переписке.

Первые разделы настроек: `OS & интеграции`, `Безопасность`, `Модели`, `Приватность`, `О приложении`.

## Уведомления

Короткое системное уведомление GNOME сообщает о завершении важной задачи. Клик по нему открывает эту панель на соответствующем чате. Подтверждение рискованных изменений выполняется только в самой панели.
