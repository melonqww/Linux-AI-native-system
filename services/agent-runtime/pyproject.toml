[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-native-linux-agent-runtime"
version = "0.1.0"
description = "Safe intent validation and policy evaluation for the portfolio MVP."
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
