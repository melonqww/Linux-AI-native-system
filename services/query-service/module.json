{
  "schema_version": 2,
  "module_id": "documents.query",
  "display_name": "Document Query Service",
  "description": "Searches allowed local files by metadata and indexed content.",
  "module_version": "0.5.0",
  "core_api": "1",
  "capabilities": [
    {
      "id": "documents.query.search",
      "description": "Search allowed local files by metadata, indexed content, type or volume.",
      "input_schema": {"type": "object", "properties": {"mode": {"type": "string", "enum": ["metadata", "content", "hybrid"], "description": "metadata lists by type or name, content searches indexed text, hybrid combines content with metadata filters"}, "text": {"type": "string", "description": "The subject or phrase required inside indexed document content. Put mathematics here for documents about mathematics and put a requested phrase here for documents containing that phrase. Never silently drop a requested subject. A destination folder name is never document content; omit text when the user only names a file type and destination."}, "content_match": {"type": "string", "enum": ["semantic", "exact_phrase"], "coRequiredWith": ["text"], "reviewChoices": {"semantic": ["semantic", "topic", "content_topic"], "exact_phrase": ["exact_phrase", "phrase", "literal_content_phrase"], "$misplaced": ["misplaced", "destination", "file_type", "other_argument"]}, "description": "Required whenever text is supplied: semantic means a topic or meaning search; exact_phrase means text itself must occur in the document. Omit only when text is absent."}, "name_terms": {"type": "array", "items": {"type": "string"}, "description": "Only terms the user explicitly requires in the file name or title. Never put a document topic or a phrase from document content here. Omit for documents about mathematics or containing a phrase unless the user also explicitly constrains the filename."}, "extensions": {"type": "array", "items": {"type": "string"}, "description": "Loss-sensitive file type restriction. When the user explicitly names a file type, return its lowercase suffix without a leading dot; uppercase spelling in the message still counts as explicit evidence."}, "volume_ids": {"type": "array", "items": {"type": "string"}, "description": "Trusted volume identifiers selected by the system"}}, "required": ["mode", "extensions"], "additionalProperties": false},
      "requested_permissions": ["filesystem.read-metadata", "filesystem.read-content"],
      "user_intent": {
        "operation": "search_documents",
        "description": "Search allowed local files. Use metadata for type/name only, content for indexed text, and hybrid for text plus filters. Keep a topic in text with content_match semantic; use exact_phrase when the user asks for a phrase occurring in a document. If text is present, always choose one content_match value. Contains/mentions means content; name_terms is only for an explicit filename.",
        "examples": ["найди все PDF файлы на компьютере", "покажи мои текстовые документы", "нужны только PDF по математике", "отфильтруй найденные документы", "filter the found documents", "show my PDF files", "отыщи мои документы", "найди документы по математике", "найди текстовый документ в котором встречается нужная фраза", "отыщи файл на диске", "find my local PDF files", "find a text document containing a phrase", "search documents on this computer"]
      }
    }
  ],
  "dependencies": ["storage.catalog", "documents.index"],
  "optional_dependencies": ["documents.pdf"],
  "requested_permissions": ["filesystem.read-metadata", "filesystem.read-content"],
  "lifecycle": "on-demand",
  "resource_class": "light",
  "default_enabled": true,
  "entrypoint": {"kind": "python-package", "module": "ai_native_query", "python_path": "src"}
}
